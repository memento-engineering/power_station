name: zero_conf_grid_assets
version: 0.3.0-rc.1
description: >-
  Zero-conf discovery and topology opinions for the office grid: mDNS/DNS-SD
  advertise and browse of station brokers, applied over federated_grid_assets.
repository: https://github.com/memento-engineering/power_station
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  federated_grid_assets: ^0.3.0-rc.1
  meta: ^1.16.0
  multicast_dns: ^0.3.3

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
