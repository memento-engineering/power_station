name: grid_assets
version: 0.6.0-rc.1
description: >-
  The_grid's opinion assets: the agent/verify/land Capability impls, the code
  circuit, and the git SourceControl, extracted out of the opinion-free engine.
repository: https://github.com/memento-engineering/power_station
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.6.0
  beads_dart: ^0.2.0-rc.1
  crypto: ^3.0.6
  dart_grid_assets: ^0.1.0
  extension_discovery: ^2.1.0
  federated_grid_assets: ^0.3.0-rc.1
  freezed_annotation: ^3.0.0
  genesis_tree: ^0.2.0
  grid_engine: ^0.3.0-rc.3
  grid_runtime: ^0.2.0-rc.1
  grid_sdk: ^0.3.0-rc.3
  json_annotation: ^4.9.0
  meta: ^1.16.0
  path: ^1.9.0
  yaml: ^3.1.2

dev_dependencies:
  build_runner: ^2.4.15
  freezed: ^3.0.0
  json_serializable: ^6.9.5
  lints: ^5.1.0
  state_notifier: ^1.0.0
  test: ^1.25.0
