- [ ] With `GRID_GITHUB_APP_KEY_MEMENTO` and `GRID_GITHUB_APP_KEY_NICHOLAS` exported to arbitrary 0600 files, the whole pack suite is green — `cd packages/github_grid_assets && dart test` reports `All tests passed!` (129 passing, 1 skipped)
- [ ] The flake is GONE under repetition, not merely quiet: 20 consecutive `cd packages/github_grid_assets && dart test test/github_app_client_assets_test.dart` runs each report `+6: All tests passed!`, with the two key variables still exported
- [ ] No awaited filesystem read survives below the fake seam — `cd packages/github_grid_assets && grep -n 'readAsString()' test/github_app_client_assets_test.dart` prints nothing (the only fixture read is the synchronous `readAsStringSync()` hoisted to library load)
- [ ] The fixed 3-round pump wait is gone and the file waits through the shared bounded helper — `cd packages/github_grid_assets && grep -c 'for (var i = 0; i < 3; i++)' test/github_app_client_assets_test.dart` prints `0`, and `grep -c "import 'support/asset_fakes.dart';" test/github_app_client_assets_test.dart` prints `1`
- [ ] A hostile ambient `GRID_GITHUB_APP_KEY_*` entry in the injected environment never reaches the resolved identity, and no path but the fixture one is ever stat'd or read — `cd packages/github_grid_assets && dart test test/github_app_client_assets_test.dart -N 'hostile ambient'` reports `All tests passed!`
- [ ] `lib/` reads `Platform.environment` in exactly ONE file — `cd packages/github_grid_assets && dart test test/environment_fence_test.dart` reports `All tests passed!`, and `grep -rn 'Platform.environment' lib` prints exactly one hit, in `lib/src/credentials.dart`
- [ ] The fence is LOUD, not decorative — appending `Platform.environment['X']` to `lib/src/http_transport.dart` makes `cd packages/github_grid_assets && dart test test/environment_fence_test.dart` FAIL, and reverting that file makes it pass again
- [ ] `GitHubSelfTrust.fromEnvironment` takes an injectable `EnvironmentReader` defaulting to `platformEnvironment`, with its three call sites migrated — `cd packages/github_grid_assets && dart test test/intake/github_self_trust_test.dart` reports `All tests passed!`
- [ ] The root-cause correction is RECORDED as a decision entry — `test -f "docs/decisions/$(date +%F)-github-app-assets-flake-is-real-io-below-the-fake-read.md"` exits 0
- [ ] House gate green — `cd packages/github_grid_assets && dart pub get && dart analyze && dart test` exits 0 with `No issues found!` and `All tests passed!`