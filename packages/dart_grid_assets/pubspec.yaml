name: dart_grid_assets
version: 0.1.2
description: >-
  The DART domain grid assets — the typed grid.dart envelope + pub dev-time
  linkage + the exported DartCommand.
repository: https://github.com/memento-engineering/power_station
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.6.0
  meta: ^1.16.0
  path: ^1.9.0
  pub_semver: ^2.1.4

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
