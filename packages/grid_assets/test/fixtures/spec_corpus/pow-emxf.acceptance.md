- [ ] Editing ONE leg alone leaves the codex-leg suite green: appending a line to `packages/grid_assets/extension/station_overlay/agents/skills/discover/SKILL.md` and running `dart test test/assets/overlay_codex_leg_test.dart` still reports `All tests passed!` (the byte-identity pin is gone in substance, not just in name)
- [ ] `packages/grid_assets/test/assets/overlay_codex_leg_test.dart` no longer asserts byte equality between the two legs: `grep -n 'readAsBytesSync\|BYTE-IDENTICAL' packages/grid_assets/test/assets/overlay_codex_leg_test.dart` returns no match (exit 1)
- [ ] The three structural checks survive unchanged — skill-id parity, SKILL.md-only, no copilot leg: `dart test test/assets/overlay_codex_leg_test.dart` reports `+3: All tests passed!` and the three test names are byte-for-byte the ones on `main`
- [ ] The test file header no longer describes the agents leg as a copy that must not drift: `grep -in 'is a COPY of the claude\|pinned byte-for-byte\|drifted copy' packages/grid_assets/test/assets/overlay_codex_leg_test.dart` returns no match (exit 1), and `grep -c 'INDEPENDENT instruction source' packages/grid_assets/test/assets/overlay_codex_leg_test.dart` returns `1`
- [ ] The reversal is RECORDED: `docs/decisions/2026-09-03-a-harness-may-carry-its-own-instructions.md` exists, carries `status: accepted`, `bead: pow-emxf` and `updates:` naming `the-worktree-overlay-scope-widens-to-every-skill-tree`, and its slug is 40 characters (at most 60)
- [ ] The ratified pow-99g entry is NOT edited: `git diff --name-only main...HEAD -- docs/decisions/2026-09-02-the-worktree-overlay-scope-widens-to-every-skill-tree.md` prints nothing
- [ ] Nothing else moves — no overlay content, no installer, no mapping, no CHANGELOG edit: `git diff --name-only main...HEAD` prints EXACTLY the two paths `docs/decisions/2026-09-03-a-harness-may-carry-its-own-instructions.md` and `packages/grid_assets/test/assets/overlay_codex_leg_test.dart`
- [ ] House gate green: `cd packages/grid_assets && dart analyze && dart test` exits 0 with `No issues found!` and `All tests passed!`