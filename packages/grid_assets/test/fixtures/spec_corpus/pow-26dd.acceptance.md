- [ ] One edit verb governing a comma/and list authors EVERY path in it: for the design `Modify `X`, `Y`, and `Z`.` the `authored` bucket holds all three paths and `declaredTestFiles` still holds all three when all three are present at the base (`cd packages/grid_assets && dart test test/track_c_declared_tests_test.dart --name "comma-and list"` → `All tests passed!`)
- [ ] An and-joined pair is authored whole and still GATES: `Create `X` and `Y`.` declares both, and a pinned diff carrying only X grades `F` with a rationale naming only Y (`cd packages/grid_assets && dart test test/track_c_declared_tests_test.dart --name "and-joined pair"` → `All tests passed!`)
- [ ] A verb that FOLLOWS the list governs the whole list: `` `X` and `Y` — both modified. `` authors both paths (`cd packages/grid_assets && dart test test/track_c_declared_tests_test.dart --name "trailing verb"` → `All tests passed!`)
- [ ] A gap that carries WORDS keeps two runs separate: `Create `U`, `V` and `W`, built on the harness `H` already uses.` authors exactly U, V and W and declares only those three — the cited harness is never swallowed by the list (`cd packages/grid_assets && dart test test/track_c_declared_tests_test.dart --name "worded gap"` → `All tests passed!`)
- [ ] A two-path `Test: dart test <R> <S>` run line stays a fallback, never authored: `authored` is empty, `fallback` holds both, an empty base declares both and a base holding both declares none — `pow-0jc` is not inverted (`cd packages/grid_assets && dart test test/track_c_declared_tests_test.dart --name "two-path run line"` → `All tests passed!`)
- [ ] The three round-1 receipts still hold: `tg-5kb`, `space-fvg` and `tg-czsf` each classify as pinned and grade `A` through `DeclaredTestsCapability.run` (`cd packages/grid_assets && dart test test/track_c_declared_tests_test.dart --name "tg-5kb|space-fvg|tg-czsf"` → `+3: All tests passed!`)
- [ ] The base-match rules are unchanged: an AMBIGUOUS base suffix stays declared, and an EMPTY base declares every base-gated path — `pow-qev`'s fail-closed posture (`cd packages/grid_assets && dart test test/track_c_declared_tests_test.dart --name "ambiguous base suffix"` → `All tests passed!`)
- [ ] Every pre-existing case in the suite still passes — the 33 already on this branch plus the 5 new ones (`cd packages/grid_assets && dart test test/track_c_declared_tests_test.dart` → `+38: All tests passed!`)
- [ ] The recorded decision states the RUN rule, and legacy ADR-0000 is untouched (`grep -q '_testPathListGap' docs/decisions/2026-09-03-declared-tests-evidence-is-scoped-to-the-path-it-governs.md && git diff --stat origin/main...HEAD -- docs/adr/ADR-0000-ai-decision-register.md` → exit 0 with EMPTY diffstat output)
- [ ] House gate green (`cd packages/grid_assets && dart analyze && dart test -j 4` → `No issues found!` then `+1114 ~1: All tests passed!`, exit 0)