- [ ] WIRE — the provision hook installs a RENDERED skill: `AgentCapability._linkWorkspace` (the ADR-0000 A1 in-store provision hook) materializes the vended `extension/station_overlay/skills/discover/SKILL.md` into `<worktree>/.claude/skills/discover/SKILL.md` with every `{{…}}` hole BOUND (the installed file contains `space search --json`, contains the bound grid home, and contains NO `{{`) → `cd packages/grid_assets && dart test test/track_h_code_extension_test.dart` → PASS (the "AgentCapability materializes the station_overlay" group).
- [ ] WIRE — the brief can `/invoke` it: the spawned agent's brief NAMES the installed skill as `` `/discover` ``, so a print-mode `claude -p` (which selects no skill on its own — ADR-0001) can invoke it explicitly → `cd packages/grid_assets && dart test test/track_h_code_extension_test.dart` → PASS (same group).
- [ ] WIRE — the overlay can never enter the bead's commit: each materialized asset dir carries a SELF-IGNORING `.gitignore` (a single `*`, which matches the `.gitignore` itself), so land's `git add -A` (`GitOps.commitAll`) stages none of it → `cd packages/grid_assets && dart test test/track_h_code_extension_test.dart` → PASS (same group).
- [ ] WIRE — the git exclusion is SCOPED to the materialized asset dirs, never a blanket `.claude/` ignore: no `<worktree>/.claude/.gitignore` and no `<worktree>/.claude/skills/.gitignore` is written, so ADR-0000 A5's post-commit residue gate (`uncommittedResidue` → plain `git status --porcelain`) still sees every other file under `.claude/` → `cd packages/grid_assets && dart test test/track_h_code_extension_test.dart` → PASS (same group).
- [ ] WIRE — NON-DESTRUCTIVE: a pre-existing `<worktree>/.claude/skills/discover/SKILL.md` survives provision byte-unchanged → `cd packages/grid_assets && dart test test/track_h_code_extension_test.dart` → PASS (same group).
- [ ] WIRE — a station override wins: `AgentCapability(overlayArgs: {'runner': 'grid', 'gridHome': '/grid/home'})` renders `grid search --json` into the worktree instead of the default `space` binding → `cd packages/grid_assets && dart test test/track_h_code_extension_test.dart` → PASS (same group).
- [ ] WIRE — an UNBOUND asset is never installed and never fails the spawn: an injected fixture overlay carrying `{{nobodyBindsThis}}` yields no target file, no throw, and no `/`-invocation line in the brief → `cd packages/grid_assets && dart test test/track_h_code_extension_test.dart` → PASS (same group).
- [ ] WIRE — the brief's EXISTING contract is intact: `buildAgentBrief` still renders the bead sections and the `pow-8dx` commit policy (the bead id only as the `trailerToken` trailer), and a spawn that installs no skills gets NO skills paragraph → `cd packages/grid_assets && dart test test/track_h_code_extension_test.dart test/track_e_reference_inflation_test.dart` → PASS.
- [ ] FORMAT: `extension/station_overlay/skills/discover/SKILL.md` exists (relocated, content unchanged) and BOTH `PackagedAssetLoader.loadSkillTemplate('discover')` and the `extension/mcp/config.yaml` manifest's `discover.path` resolve the new path → `cd packages/grid_assets && dart test test/assets/skill_assets_test.dart` → PASS.
- [ ] LIB — fresh expand: `OverlayMaterializer` mirrors an overlay root's `skills/` + `agents/` subtrees (including a nested multi-file asset dir) onto an empty target as `<target>/<kind>/<relative path>` → `cd packages/grid_assets && dart test test/assets/overlay_materializer_test.dart` → PASS ("fresh expand" group).
- [ ] LIB — NON-DESTRUCTIVE (positive control): an existing target file whose bytes DIFFER from the overlay's is unchanged after materialization and is reported as `OverlayFileSkipped` → `cd packages/grid_assets && dart test test/assets/overlay_materializer_test.dart` → PASS ("non-destructive" group).
- [ ] LIB — RENDER + REFUSE: declared args are substituted into each copied file, and a file left with an unbound `{{hole}}` is REFUSED (`OverlayFileRefused`, naming the hole) and NOT written — a half-bound asset never reaches an agent, and `installedSkillIds` never names it → `cd packages/grid_assets && dart test test/assets/overlay_materializer_test.dart` → PASS ("render + refuse" group).
- [ ] LIB — UNION BY TREE POSITION: two roots offering distinct assets both land; two roots offering the SAME path yield only the EARLIER-listed root's content (first tree position wins, via the same skip-existing mechanism) → `cd packages/grid_assets && dart test test/assets/overlay_materializer_test.dart` → PASS ("union by tree position" group).
- [ ] LIB — the FORMAT is enforced LOUD: a file sitting loose directly under a kind dir (not in its own `<kind>/<name>/` asset dir) THROWS a `StateError` naming the path — a malformed vended tree is a packaging bug, never a silent half-install → `cd packages/grid_assets && dart test test/assets/overlay_materializer_test.dart` → PASS ("the format is enforced LOUD" group).
- [ ] LIB — EDGE CASES: an overlay root with neither `skills/` nor `agents/`, and an empty `overlayRoots` list, each yield an empty report with no throw → `cd packages/grid_assets && dart test test/assets/overlay_materializer_test.dart` → PASS (the two edge-case tests in "fresh expand").
- [ ] LIB — sync/async parity: `materializeSync` (the entry point the WIRE rides, since `ProcessCapability.spawn` cannot await) produces the same outcome as `materialize` for identical input → `cd packages/grid_assets && dart test test/assets/overlay_materializer_test.dart` → PASS.
- [ ] LIB — the real `extension/station_overlay` round-trips through the generic lib: materializing it with `{'runner': 'space', 'gridHome': '/grid/home'}` writes a real, non-empty, `---`-frontmatter-led `skills/discover/SKILL.md` with no `{{` residue → `cd packages/grid_assets && dart test test/assets/overlay_materializer_test.dart` → PASS ("the live grid_assets station_overlay" group).
- [ ] LIB — NO CLI dependency (UI-drivable; `pow-a74` is the only CLI seam) and NO git dependency (the operator's `~/.claude` is not a repo): `overlay_materializer.dart` references neither `package:args`/`CommandRunner` nor `GitRunner` → `cd packages/grid_assets && dart test test/assets/overlay_materializer_test.dart` → PASS ("no CLI and no git dependency" test).
- [ ] No regression: the package's whole existing suite still passes after the relocation, the new lib, and the wire → `cd packages/grid_assets && dart test` → PASS, 0 failures.
- [ ] Static analysis is clean under the house strict-casts/-inference/-raw-types lints → `cd packages/grid_assets && dart analyze` → `No issues found!`