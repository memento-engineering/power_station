name: federated_grid_assets
version: 0.1.0
description: >-
  Cross-station federation impls: the HTTP station bus, the owner-authoritative
  lease manager, static membership, git-over-LAN sync, and the serve/lease CLI
  commands.
repository: https://github.com/memento-engineering/power_station
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.6.0
  genesis_tree: ^0.1.3
  grid_engine: ^0.1.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
