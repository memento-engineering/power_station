name: analytical_grid_assets
version: 0.1.0
description: >-
  The ANALYTICAL domain grid assets — station health and effectiveness
  reporting over the_grid's typed session-ledger metrics projection: pure view
  models, split token distributions, and the exported MetricsCommand.
repository: https://github.com/memento-engineering/power_station
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.6.0
  beads_dart: ^0.2.0-rc.7
  grid_assets: ^0.6.0-rc.10
  grid_engine: ^0.3.0-rc.14
  grid_runtime: ^0.2.0-rc.10
  grid_sdk: ^0.3.0-rc.10
  path: ^1.9.0

dev_dependencies:
  genesis_tree: ^0.3.0
  lints: ^5.1.0
  test: ^1.25.0
